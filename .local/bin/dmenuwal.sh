#!/bin/sh
# This script changes the color to pywal colors when run. I use it with ranger when a wallpaper needs to be set. please see the ranger config
#
# Import the colors
#. "${HOME}/.cache/wal/colors.sh"
#
#dmenu_run -nb "$color0" -nf "$color15" -sb "$color1" -sf "$color15"

cp ~/.cache/wal/.dmenurc ~/
