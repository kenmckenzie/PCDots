#this is gonna be the config.fish file to set pywal colors

#set -x fish_color_normal {color3.strip} # Normal colors
#set -x fish_color_command {color13.strip} # the color for commands
set -x fish_color_quote {color3.strip} # the color for quoted blocks of text
#set -x fish_color_redirection {color10.strip}  # the color for IO redirections
#set -x fish_color_end {color10.strip} # the color for process separators like ';' and '&'
#set -x fish_color_error {color1.strip} # the color used to highlight potential errors
#set -x fish_color_param {color13.strip} # the color for regular command parameters
#set -x fish_color_comment {color3.strip} # the color used for code comments
#set -x fish_color_match {color4.strip} #  the color used to highlight matching parenthesis
#set -x fish_color_selection {color5.strip} # the color used when selecting text (in vi visual mode)
#set -x fish_color_search_match {color1.strip} #, used to highlight history search matches and the selected pager item (must be a background)
#set -x fish_color_operator {color2.strip} # the color for parameter expansion operators like '*' and '~'
#set -x fish_color_escape {color3.strip} # the color used to highlight character escapes like '\n' and '\x70'

#set -x fish_color_cwd {color4.strip} # the color used for the current working directory in the default prompt
#set -x fish_color_autosuggestion {color13.strip} # the color used for autosuggestions
#set -x fish_color_user {color1.strip} # the color used to print the current username in some of fish default prompts
#set -x fish_color_host {color2.strip} # the color used to print the current host system in some of fish default prompts
#set -x fish_color_cancel{color3.strip} # the color for the '^C' indicator on a canceled command
